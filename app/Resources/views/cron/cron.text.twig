Hello,

{{ indents|length }} order{{ indents|length > 1 ? 's' }} to process:

{% for indent in indents %}

    Distributor: {{ indent.getProduct.getDistributor.getName }}
    Description: {{ indent.getProduct.getName }}
    Price: {{ indent.getProduct.getPrice }} {{ indent.getProduct.getCurrency }}
    Reference: {{ indent.getProduct.getReference }}
    Quantity: {{ indent.getQuantity }}
    User: {{ indent.getUser }}
    {% if indent.getProduct.getQuote %}
    Quote: See attached file for the quote
    {% endif %}

===============================================================
{% endfor %}

Thanks!

Have a nice day :)
